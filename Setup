This project is a Java-based Blood Bank Management System designed to help manage blood donations and users. It allows users to register, log in, and view available blood banks. The system connects to a MySQL database for storing user data and managing blood donation records. The project includes functionality for creating, reading, updating, and deleting records. It uses JDBC (Java Database Connectivity) for communication with the MySQL database and is built using Java, HTML, and CSS. This project aims to provide an efficient solution for managing blood donations in real-time.

